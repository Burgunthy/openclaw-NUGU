# Contributing to NUGU + OpenClaw + Home Assistant Integration

기여해 주셔서 감사합니다! 이 프로젝트에 기여하는 방법을 안내합니다.

## 기여 방법

### 버그 신고

1. [Issues](https://github.com/yourusername/nugu-openclaw-ha/issues)에서 버그 신고
2. 제목에 `[Bug]` 태그 추가
3. 다음 정보 포함:
   - 사용 환경 (OS, Home Assistant 버전, OpenClaw 버전)
   - 재현 단계
   - 에러 메시지 또는 로그
   - 스크린샷 (가능한 경우)

### 기능 제안

1. [Issues](https://github.com/yourusername/nugu-openclaw-ha/issues)에서 제안
2. 제목에 `[Feature]` 태그 추가
3. 기능에 대한 상세 설명
4. 사용 사례 (Use case)

### 풀 리퀘스트

1. Fork 후 브랜치 생성:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. 변경사항 커밋:
   ```bash
   git commit -m "Add: Your feature description"
   ```

3. 푸시 후 PR 생성:
   ```bash
   git push origin feature/your-feature-name
   ```

4. PR 제목에 접두사 추가:
   - `Add:` 새로운 기능
   - `Fix:` 버그 수정
   - `Docs:` 문서 수정
   - `Refactor:` 리팩토링
   - `Test:` 테스트 추가

### 코딩 컨벤션

- Python: PEP 8 따르기
- YAML: 2칸 들여쓰기
- 주석: 복잡한 로직에는 설명 추가
- 커밋 메시지: 한국어 또는 영어 사용

### 테스트

- 변경사항 테스트
- 다양한 환경에서 테스트 (가능한 경우)
- 문서 업데이트

## 라이선스

모든 기여는 MIT 라이선스 하에 이루어집니다.
